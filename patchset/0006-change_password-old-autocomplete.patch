*** Begin Patch
*** Update File: /var/www/html/moodle/public/login/change_password_form.php
@@
-        $purpose = user_edit_map_field_purpose($USER->id, 'password');
-        $mform->addElement('password', 'password', get_string('oldpassword'), $purpose);
+        $purpose = user_edit_map_field_purpose($USER->id, 'password');
+        // Ensure browsers treat this as the current password field for autocomplete.
+        if (is_array($purpose)) {
+            $purpose['autocomplete'] = 'current-password';
+        } else {
+            $purpose = trim($purpose . ' autocomplete="current-password"');
+        }
+        $mform->addElement('password', 'password', get_string('oldpassword'), $purpose);
*** End Patch
