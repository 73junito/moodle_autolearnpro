*** Begin Patch
*** Update File: /var/www/html/moodle/public/user/editadvanced_form.php
@@
-        $purpose = user_edit_map_field_purpose($userid, 'password');
-        $mform->addElement('passwordunmask', 'newpassword', get_string('newpassword'),
-            'maxlength="'.MAX_PASSWORD_CHARACTERS.'" size="20"' . $purpose);
+        $purpose = user_edit_map_field_purpose($userid, 'password');
+        // Encourage browsers to treat this as a new user password input for autocomplete.
+        if (is_array($purpose)) {
+            $purpose['autocomplete'] = 'new-password';
+        } else {
+            $purpose = trim($purpose . ' autocomplete="new-password"');
+        }
+        $mform->addElement('passwordunmask', 'newpassword', get_string('newpassword'),
+            'maxlength="'.MAX_PASSWORD_CHARACTERS.'" size="20"' . $purpose);
*** End Patch
